function listenForInfoButton() {
  const infoButtons = document.querySelectorAll('.general-info-button')
  
  document.addEventListener('mouseover', event => {
    if (event.target.classList.contains('general-info-button')) {
      
    }
  });
}
